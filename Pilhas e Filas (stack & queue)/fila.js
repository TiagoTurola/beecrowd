//Uma fila (queue) tipicamente estabelece uma política FIFO -- first in, first out
const fila = [1, 5, 7, 9, 2];

const add = (item) => {
  fila.push(item);
};

const remove = () => {
  pilha.shift();
};
